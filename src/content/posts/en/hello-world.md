---
title: Hello, world
date: 2026-08-20
description: A template post — delete this file once you have written a real one.
draft: true
---

This file is a template. It lives in `src/content/posts/en/`, and the file name
(`hello-world.md`) becomes its URL: `/blog/hello-world`.

Because `draft: true` is set in the frontmatter above, it shows up when you run
`npm run dev` but never in the built site. Change it to `draft: false` to
publish. The `description` line is optional; it appears under the title in the
post list and as the page description for search engines.

## What you can write

Regular paragraphs, **bold**, *italics*, a [link](https://astro.build), and
`inline code`. Headings start at `##` — the post title above is already the `#`.

- Bullet lists work
- So do numbered ones

> A blockquote, for a sentence worth pulling out of the flow.

Code blocks are syntax-highlighted:

```python
def surrogate(mesh):
    return model(mesh)
```

---

To write a new post, copy this file, give it a new name, and start typing. The
list orders by `date`, newest first.
